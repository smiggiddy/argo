apiVersion: barmancloud.cnpg.io/v1
kind: ObjectStore
metadata:
  name: nextcloud-pg
  namespace: nextcloud
spec:
  configuration:
    destinationPath: "s3://cnpg-backups/nextcloud-pg"
    endpointURL: http://minio.minio-tenant
    s3Credentials:
      accessKeyId:
        name: nextcloud-backup-credentials
        key: ACCESS_KEY_ID
      secretAccessKey:
        name: nextcloud-backup-credentials
        key: ACCESS_SECRET_KEY
    wal:
      compression: gzip
  retentionPolicy: "30d"
---
apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: nextcloud-pg
  namespace: nextcloud
spec:
  schedule: "0 0 7 * * 5"
  backupOwnerReference: self
  cluster:
    name: nextcloud-pg
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
