# https://kubernetes.io/docs/concepts/services-networking/service/
apiVersion: v1
kind: Service
metadata:
  name: hermes-agent
spec:
  selector:
    app: hermes-agent
  type: ClusterIP
  ports:
    - name: hermes-agent
      protocol: TCP
      port: 8642
      targetPort: 8642
