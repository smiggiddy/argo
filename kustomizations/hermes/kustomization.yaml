resources:
  - ./deployment.yaml
  - ./service.yaml
  - ./secret.yaml

images:
  - name: nousresearch/hermes-agent
    newTag: latest

configMapGenerator:
  - name: hermes-configmap-1
    envs:
      - ./configmap.yaml
