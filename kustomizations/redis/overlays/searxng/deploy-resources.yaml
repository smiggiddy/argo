apiVersion: apps/v1
kind: Deployment
metadata:
  name: searxng
spec:
  template:
    spec:
      containers:
        - name: valkey
          resources:
            requests:
              cpu: 100m
              memory: "64Mi"
            limits:
              cpu: 150m
              memory: "128Mi"
