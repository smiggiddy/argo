# This ConfigMap is generated by kustomization.yaml from config.env
# The actual ConfigMap will have a hash suffix for rolling updates
# This file can be removed if you prefer pure generator approach
apiVersion: v1
kind: ConfigMap
metadata:
  name: home-assistant-config
data:
  TZ: "America/New_York"
