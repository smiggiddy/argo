# Identity the Vault Agent authenticates with. Must match the bound service
# account name in the Vault Kubernetes auth role `searxng` (see README notes in
# the deployment annotations).
apiVersion: v1
kind: ServiceAccount
metadata:
  name: searxng
  labels:
    app: searxng
