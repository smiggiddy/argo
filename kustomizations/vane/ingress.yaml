apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: perplexica
  annotations:
    cert-manager.io/cluster-issuer: cluster-ca-issuer
    external-dns.alpha.kubernetes.io/target: k3s.lab.smig.tech.
spec:
  tls:
    - hosts:
        - vane.lab.smig.tech
      secretName: perplexica-tls
  rules:
    - host: vane.lab.smig.tech
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: vane
                port:
                  name: http
