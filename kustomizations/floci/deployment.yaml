# https://kubernetes.io/docs/concepts/workloads/controllers/deployment/
apiVersion: apps/v1
kind: Deployment
metadata:
  name: floci
  labels:
    app: floci
spec:
  selector:
    matchLabels:
      app: floci
  replicas: 1
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/default-container: floci
      labels:
        app: floci
    spec:
      containers:
        - name: floci
          image: hectorvent/floci:latest@sha256:c969cb48fcfae0eff3f4a4647b7772cd975e633cae7468e45abaf2d2eae18558
          imagePullPolicy: IfNotPresent
          resources:
            requests:
              cpu: 100m
              memory: 100Mi
            limits:
          envFrom:
            - configMapRef:
                name: floci-config
          ports:
            - containerPort: 4566
              name: floci
          volumeMounts:
            - name: data
              mountPath: /app/data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: floci
      restartPolicy: Always
      securityContext:
        fsGroup: 1001
