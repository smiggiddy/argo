apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: the-method-pg
  namespace: smigz
spec:
  imageCatalogRef:
    apiGroup: postgresql.cnpg.io
    kind: ClusterImageCatalog
    name: postgresql
    major: 17
  instances: 1
  # single instance: the default PDB has minAvailable=1 against 1 pod, so it
  # allows 0 disruptions and blocks node drains forever while protecting
  # nothing -- there is no second copy to keep serving.
  enablePDB: false
  storage:
    size: 5Gi
  resources:
    requests:
      memory: "256Mi"
      cpu: "100m"
    limits:
      memory: "512Mi"
      cpu: "1"
  bootstrap:
    initdb:
      database: the_method
      owner: the_method
      encoding: UTF8
      localeCType: C
      localeCollate: C
      secret:
        name: the-method-postgres-credentials

  plugins:
    - name: barman-cloud.cloudnative-pg.io
      enabled: true
      isWALArchiver: true
      parameters:
        barmanObjectName: the-method-pg
