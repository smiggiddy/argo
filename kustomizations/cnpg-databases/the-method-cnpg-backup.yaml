apiVersion: barmancloud.cnpg.io/v1
kind: ObjectStore
metadata:
  name: the-method-pg
  namespace: smigz
spec:
  configuration:
    destinationPath: "s3://cnpg-backups"
    endpointURL: http://garage.garage:3900
    s3Credentials:
      accessKeyId:
        name: the-method-backup-credentials
        key: ACCESS_KEY_ID
      secretAccessKey:
        name: the-method-backup-credentials
        key: ACCESS_SECRET_KEY
      region:
        key: region
        name: the-method-backup-credentials
    wal:
      compression: gzip
  retentionPolicy: "7d"
---
apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: the-method-pg
  namespace: smigz
spec:
  schedule: "0 0 * * *"
  backupOwnerReference: self
  cluster:
    name: the-method-pg
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
