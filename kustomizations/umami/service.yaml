apiVersion: v1
kind: Service
metadata:
  labels:
    app: umami-svc
  name: umami-svc
spec:
  ports:
    - name: 3000-3000
      port: 3000
      protocol: TCP
      targetPort: 3000
  selector:
    app: umami
  type: ClusterIP
status:
  loadBalancer: {}
